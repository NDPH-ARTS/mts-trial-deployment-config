spring:
  liquibase:
    change-log: classpath:db/changelog/changelog-role-service-mastera.xml 
    drop-first: false
    enabled: true
  jpa:
    hibernate:
      ddl-auto: none
  hateoas:
    use-hal-as-default-json-media-type: false
server:
  error:
    include-message: always
    include-binding-errors: always
