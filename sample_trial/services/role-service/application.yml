spring:
  liquibase:
    change-log: ${LIQUIBASE_LOC}
    drop-first: false
    enabled: true
  jpa:
    hibernate:
      ddl-auto: none
  hateoas:
    use-hal-as-default-json-media-type: false
server:
  error:
    include-message: always
    include-binding-errors: always
